# schema: https://paynroll-server.onrender.com/graphql
overwrite: true
schema: http://localhost:9000/graphql
documents:
  - './src/**/*.ts'
generates:
  src/generated-types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-apollo-angular